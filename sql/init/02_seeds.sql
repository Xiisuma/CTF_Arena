
-- 02_seeds.sql supprimé v4.2
-- Les données par défaut (catégories, challenges, achievements) sont désormais
-- importées via l'interface admin : Paramètres → Import / Export.
-- Pour initialiser une nouvelle instance, importer archive/default_data.json.
USE ctf_arena;

